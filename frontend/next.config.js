/** @type {import('next').NextConfig} */
const nextConfig = {
  // Produces a self-contained build under .next/standalone
  // that can be run with: node .next/standalone/server.js
  output: 'standalone',
};

module.exports = nextConfig;
